# Notes
Notes app on ```Vue.js``` with a database on ```python```
### How to run a project locally
1. Clone the repository ```git clone https://github.com/qwizzit/simple-note```
2. Install dependencies ```npm install```
3. Run the project in the terminal ```npm run dev```