{
    "name": "noteshare",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
      "frontend"
    ],
    "scripts": {
      "frontend:dev": "npm run dev --workspace=frontend",
      "backend:dev": "cmd /c \"cd backend && venv\\Scripts\\python -m uvicorn app.main:app --reload\"",
      "dev": "npm-run-all --parallel frontend:dev backend:dev"
    },
    "devDependencies": {
      "npm-run-all": "^4.1.5"
    }
  }